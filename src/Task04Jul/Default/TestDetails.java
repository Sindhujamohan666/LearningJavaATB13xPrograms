package Task04Jul.Default;

public class TestDetails {
    public static void main(String[] args) {
        //Student stud=new Student();
       // stud.showDetails();
       /* show details is default ..it cannot be accessed in diff package with only obj creation
       Package has to b imported.
        */
    }
}
